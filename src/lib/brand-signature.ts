/**
 * Route5 signature brand — concise, memorable, used in shell chrome.
 * Aligns with SaaS wordmark + single-accent practice (distinctive “5”, route metaphor).
 */
export const ROUTE5_SIGNATURE = {
  /** Primary wordmark text (always with numeral 5). */
  wordmark: "Route5",
  /** One line under the logo — “identification over explanation” for recall. */
  tagline: "Execution you can see.",
  /** Longer kicker for marketing / About (optional). */
  story:
    "A clear path from decision to done — commitments stay owned, visible, and measurable.",
} as const;
