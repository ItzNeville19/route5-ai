/**
 * Canonical signed-in routes. Feed is the primary commitment surface; Leadership
 * (morning command view) lives at /overview until a dedicated route ships; Desk is /desk.
 */
export const FEED_HREF = "/feed" as const;
export const OVERVIEW_HREF = "/overview" as const;
