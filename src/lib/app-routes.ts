/**
 * Canonical signed-in routes. Overview is the morning dashboard (execution health,
 * risk, team load); project detail stays under /projects/[id]; Desk is /desk.
 */
export const OVERVIEW_HREF = "/overview" as const;
