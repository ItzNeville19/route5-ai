/** Reserved for rare edge cases — product flows use live + walkthrough data instead of “not configured” walls. */
export const INTEGRATION_USER = {
  tryAgain: "Something didn’t load. Tap refresh.",
} as const;
